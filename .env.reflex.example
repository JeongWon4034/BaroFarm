# EC2에서 docker-compose.reflex-prod.yml 실행 시 사용
# cp .env.reflex.example .env.reflex 후 값 채우기
# 실행: docker compose -f docker-compose.reflex-prod.yml --env-file .env.reflex up -d --build

EC2_PUBLIC_IP=<EC2 퍼블릭 IP 주소>
MYSQL_ROOT_PASSWORD=<강한 비밀번호>
