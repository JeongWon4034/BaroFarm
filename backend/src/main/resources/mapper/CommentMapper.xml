<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freshgrowth.comment.CommentMapper">
    <insert id="insert" parameterType="com.freshgrowth.comment.Comment" useGeneratedKeys="true" keyProperty="commentId">
        INSERT INTO comments(post_id, parent_id, author_id, content)
        VALUES (#{postId}, #{parentId}, #{authorId}, #{content})
    </insert>

    <sql id="cols">
        c.comment_id, c.post_id, c.parent_id, c.author_id, u.name AS author_name,
        c.content, c.created_at, c.updated_at
    </sql>

    <select id="findById" resultType="com.freshgrowth.comment.Comment">
        SELECT <include refid="cols"/>
        FROM comments c JOIN users u ON c.author_id = u.user_id
        WHERE c.comment_id = #{commentId}
    </select>

    <select id="findByPostId" resultType="com.freshgrowth.comment.Comment">
        SELECT <include refid="cols"/>
        FROM comments c JOIN users u ON c.author_id = u.user_id
        WHERE c.post_id = #{postId}
        ORDER BY COALESCE(c.parent_id, c.comment_id) ASC,
                 (c.parent_id IS NOT NULL) ASC,
                 c.comment_id ASC
    </select>

    <update id="update">
        UPDATE comments
        SET content = #{content}
        WHERE comment_id = #{commentId} AND author_id = #{authorId}
    </update>

    <delete id="delete">
        DELETE FROM comments
        WHERE comment_id = #{commentId} AND author_id = #{authorId}
    </delete>
</mapper>
