<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freshgrowth.event.EventMapper">
    <insert id="insert" parameterType="com.freshgrowth.event.BehaviorEvent"
            useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO user_behavior_logs
            (session_id, user_id, event_type, product_id, ab_test_group, device_type, stay_duration, occurred_at)
        VALUES
            (#{sessionId}, #{userId}, #{eventType}, #{productId}, #{abTestGroup}, #{deviceType}, #{stayDuration}, #{occurredAt})
    </insert>
</mapper>
