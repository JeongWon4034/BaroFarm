<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freshgrowth.user.UserMapper">
    <insert id="insert" parameterType="com.freshgrowth.user.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users(role, email, password, name, status)
        VALUES (#{role}, #{email}, #{password}, #{name}, #{status})
    </insert>

    <select id="findById" resultType="com.freshgrowth.user.User">
        SELECT user_id, role, email, password, name, intro, phone, profile_image, status, created_at
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findByEmail" resultType="com.freshgrowth.user.User">
        SELECT user_id, role, email, password, name, intro, phone, profile_image, status, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <update id="updateProfile" parameterType="com.freshgrowth.user.User">
        UPDATE users
        SET name = #{name}, intro = #{intro}, phone = #{phone}, profile_image = #{profileImage}
        WHERE user_id = #{userId}
    </update>

    <update id="deactivate">
        UPDATE users
        SET status = 'INACTIVE'
        WHERE user_id = #{userId}
    </update>
</mapper>
